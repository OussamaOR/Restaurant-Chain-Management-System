#pragma once
#include "DailySale.h"

class salesBST
{
    DailySale root;
    // isertion
    // deletion + useful funcions for BST (ISEMPTY() , CONTAINS()..,ETC)
    // function to return sales at a sepcific date
    /***  the following three functions are similar but implementing them seperatly is better ***/
    // function to return sales on a specific period
    // function to return total sales on a month
    // function to return total sales on a year
    //function to return min and max (daily sale) (you can add it for the max sale on a month if you see it's useful)
};
